{
  "name": "exiuscart",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.2.0",
  "description": "ExiusCart - Smart Multi-Shop Business System for UAE",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "dev:website": "turbo run dev --filter=@exiuscart/website",
    "dev:store": "turbo run dev --filter=@exiuscart/store",
    "dev:admin": "turbo run dev --filter=@exiuscart/admin",
    "dev:affiliates": "turbo run dev --filter=@exiuscart/affiliates",
    "dev:trends": "turbo run dev --filter=@exiuscart/trends",
    "dev:api": "cd exiuscart-backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "db:migrate": "cd exiuscart-backend && alembic upgrade head",
    "db:makemigrations": "cd exiuscart-backend && alembic revision --autogenerate -m"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.2.0",
    "@types/node": "^20.11.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
